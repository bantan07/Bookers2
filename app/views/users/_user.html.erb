   <h2>User info</h2>
      <%= attachment_image_tag user, :image, :fill, 100, 100,  fallback: "no_image.jpg" %>
      <% if user.id == current_user.id %>
      <h4>name</h4>
      <%= user.name %>
      <h4>introduction</h4>
      <%= user.introduction %>
         
      <%= link_to "user info", edit_user_path(user) %>
   <% end %>
